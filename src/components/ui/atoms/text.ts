import { styled, Typography } from '@mui/material'

export const Logo = styled(Typography)({
  color: '#F2EDED',
  fontSize: 45,
  fontFamily: 'Noto Serif, serif',
})

export const CatchCopyText = styled(Typography)({
  fontSize: '5vw',
  color: '#0b0c0e',
})

export const H2 = styled(Typography)({
  fontSize: 40,
})
